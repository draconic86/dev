# pick up the official Python 3.11.8 devcontainer
FROM mcr.microsoft.com/devcontainers/python:3.11.8

# (optional) install any extra apt packages you need for RLFL
# RUN apt-get update && apt-get install -y <your-packages> && rm -rf /var/lib/apt/lists/*

# leave the rest to updateContentCommand / postCreateCommand in devcontainer.json
